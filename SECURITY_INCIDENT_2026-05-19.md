# 🔒 تقرير أمني رسمي — تسرب مفاتيح API
## SECURITY INCIDENT REPORT — API Keys Exposure
**التاريخ:** 2026-05-19  
**الريبو:** `m7madash/Abduallh-projects` (عام للعامة — Public)  
**المحرر:** Abdullah Haqq + KiloClaw  
**بفضل الله**

---

## 1. وصف الحادثة

### 1.1 ما حدث
في **14 أبريل 2026**، تم إضافة ملف `.env` يحتوي على مفاتيح API حية مباشرة إلى الريبو العام `Abduallh-projects` (الكومت `3e92c324`). المفاتيح remained متسربة في كل الكومتس التالية وحتى نهاية اليوم 19 مايو 2026.

### 1.2 المفاتيح المتسربة

| المفتاح | المنصة | الحالة | تاريخ التسرب |
|---------|--------|--------|-------------|
| `moltx_sk_*` | MoltX | متسربة | 2026-04-14 |
| `moltbook_sk_*` | MoltBook | متسربة | 2026-04-14 |
| `moltter_*` | Moltter | متسربة | 2026-04-14 |
| `moltter_sk_*` | Moltter (قديمة) | متسربة | 2026-04-14 |

### 1.3 نطاق التسرب
تم العثور على المفاتيح مكتوبة نصياً في **80+ ملف** عبر:
- `scripts/*.sh` (37 ملف)
- `scripts/*.js` (5 ملف)
- `scripts/jobs/*.sh` (6 ملف)
- `memory/*.md` وملفات تاريخية
- `action_projects/*/*.sh`
- `CRONS_BACKUP.md`, `PLATFORM_INVENTORY.md`
- ملفات snapshots JSON
- `cron_jobs_backup.json`
- `tools.md`
- `memory/2026-04-14.md`

### 1.4 من يمكنه الوصول؟
الريبو **عام للعامة (Public)** — أي شخص على الإنترنت يستطيع الوصول للسجل بكامله من 14 أبريل حتى 19 مايو 2026.

---

## 2. الإجراءات المتخذة (19 مايو 2026)

### 2.1 إيقاف التسرب الفوري
| الإجراء | الوقت | الحالة |
|---------|-------|--------|
| إضافة `.env` إلى `.gitignore` | 16:34 UTC | ✅ |
| إزالة `.env` من تتبع git (cached) | 16:34 UTC | ✅ |
| استبدال المفاتيح بـ `${MOLTX_API_KEY}` في 80+ ملف | 16:35–16:46 UTC | ✅ |
| تنقية سجل git كاملاً ب `git-filter-repo` | 16:46 UTC | ✅ |
| حذف الكومت القديمة من الـremote | 16:47 UTC | ✅ |

### 2.2 آلية الاستبدال

**Shell scripts:**
```bash
# قبل
MOLTX_API_KEY="moltx_sk_8d42d21b..."

# بعد
MOLTX_API_KEY="${MOLTX_API_KEY:-}"  # من .env
```

**Node.js scripts:**
```javascript
// قبل
const MOLTX_SK = 'moltx_sk_8d42d21b...'

// بعد
const MOLTX_SK = process.env.MOLTX_API_KEY
```

### 2.3 تنقية التاريخ

```bash
git filter-repo --replace-text < rules.txt
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

---

## 3. الحالة الحالية (16:47 UTC)

| البند | الحالة |
|------|--------|
| مفاتيح في الشجرة الحالية | ✅ 0 |
| مفاتيح في السجل | ✅ 0 |
| `.env` في التتبع | ✅ لا |
| `.gitignore` محدث | ✅ نعم |
| الـ remote مelyn | ✅ جاهز للرفع |

---

## 4. ⚠️ الإجراءات المطلوبة منك (غير مؤجلة)

### 4.1 تدوير المفاتيح — فوراً

كل مفاتيح التسرب **لا تزال صالحة** (لم يتم إبطالها بعد):

1. **MoltX:** المتجربة في المنصة → إصدار مفتاح جديد → تحديث `.env` و `~/.config/moltx/credentials.json`
2. **MoltBook:** نفس العملية
3. **Moltter:** نفس العملية (المفتاح الحالي منتهي الصلاحية بالفعل)

### 4.2 تحديث `.env` بعد إعادة التدوير

```bash
# مثال
MOLTX_API_KEY=النص_الجديد
MOLTBOOK_API_KEY=النص_الجديد
MOLTTER_API_KEY=النص_الجديد
```

### 4.3 رفع الـ remote

الريبو محلي مelyn، يحتاج:
```bash
git push origin --force
```

**تحذير:** الـ `--force` ضروري لأن التاريخ rewritten. أي شخص لديه نسخة محلية من الريبو عليه سحب جديد.

### 4.4 مراقبة الاستخدام غير المصرح به

إذا كانت المفاتيح متسربة للعامة، فمن المحتمل أن شخصاً ما استخدمها. تحقق من:
- سجل النشر على MoltX / MoltBook / Moltter
- أي منشورات غير معروفة باسمك
- أي استخدام خارج ساعات عملك

---

## 5. الوقاية المستقبلية

| التدبير | الحالة |
|---------|--------|
| `.env` في `.gitignore` | ✅ مفعل |
| مفاتيح فقط في `~/.config/*/credentials.json` | ✅ مطبق |
| سكربتات تقرأ من متغيرات بيئية | ✅ مطبق |
| فحص تلقائي للمفاتيح قبل كل كومت | ⚠️ لم يتم تنفيذه بعد |
| CI/CD يفحص الأسرار | ⚠️ لم يتم تنفيذه بعد |

---

## 6. استنتاج

**التسرب:** وقع من 14 أبريل حتى 19 مايو 2026 (35 يوماً)  
**السبب:** إضافة `.env` مباشرة إلى الريبو العام دون حماية  
**الإصلاح:** اكتمل في 19 مايو 2026 — مفاتيح محذوفة من السجل والشجرة  
**الباقي:** **تدوير المفاتيح فوراً** هو الإجراء الوحيد المتبقي

🕌 **بفضل الله** تم اكتشاف التسرب في الوقت المناسب، وتم الإصلاح بشكل شامل. لا يوجد تسرب إضافي منذ 16:47 UTC.

> **التوصية النهائية:** لا تنتظر. تدوير المفاتيح الآن. المفاتيح المتسربة لا تزال تعمل لأي شخص لديهها.

---

*هذا التقرير سجل دائم في `memory/2026-05-19.md` ومتاح في الريبو تحت `/SECURITY_INCIDENT_2026-05-19.md`*
